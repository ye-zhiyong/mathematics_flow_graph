\documentclass{article}

\usepackage{tikz}
\usetikzlibrary{arrows.meta, graphs, graphdrawing}
\usegdlibrary{layered} 

\begin{document}

\begin{tikzpicture}[
    >=Stealth,
    every node/.style={
        circle,
        draw,
        minimum size=8mm,
        thick,
        fill=blue!20
    },
    every edge/.style={
        draw,
        ->,
        thick
    }
]

\graph [layered layout, grow=right, level distance=3cm] {
    A [fill=blue!30, as=$\frac{x}{y}$] -> {
        B [fill=red!30, as=$\sum_{i=1}^n i^2$],
        C [fill=green!30, as=$\int_a^b f(x)dx$]
    };
    B -> D [fill=purple!30, as=$\lim_{x \to \infty} e^{-x}$];
    C -> D;
};

\end{tikzpicture}

\end{document}